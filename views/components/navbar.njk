<nav class="sb-topnav navbar">
  <a class="navbar-brand" href="/">
    <i class="fas fa-store me-2" style="color:var(--accent)"></i>Funcode POS
  </a>
  <button class="btn btn-ghost btn-sm ms-2" id="sidebarToggle" type="button" aria-label="Toggle sidebar">
    <i class="fas fa-bars"></i>
  </button>

  <div class="ms-auto d-flex align-items-center gap-2">
    <li class="nav-item dropdown list-unstyled mb-0">
      <a class="nav-user-btn dropdown-toggle" href="#" role="button"
         data-bs-toggle="dropdown" aria-expanded="false" id="navbarDropdown">
        <span class="nav-user-avatar">{{ user.username[0] | upper if user.username else 'U' }}</span>
        <span class="d-none d-md-block fw-medium" style="font-size:.8125rem; color:var(--text-1)">{{ user.username }}</span>
      </a>
      <ul class="dropdown-menu dropdown-menu-end" aria-labelledby="navbarDropdown">
        <li>
          <span class="dropdown-item disabled" style="cursor:default; color:var(--text-3)">
            <i class="fas fa-shield-alt"></i> {{ user.role }}
          </span>
        </li>
        <li><hr class="dropdown-divider"></li>
        <li>
          <form action="/logout" method="post">
            <button class="dropdown-item text-danger" type="submit">
              <i class="fas fa-sign-out-alt"></i> Keluar
            </button>
          </form>
        </li>
      </ul>
    </li>
  </div>
</nav>
